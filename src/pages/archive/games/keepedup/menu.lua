
local Menu = class("Menu")

function Menu:initialize()
end

function Menu:update(dt)
end

function Menu:draw()
    local wMenu = 400
    local yMenu = 200
    local xMenu = love.graphics.getWidth()/2
    local yMenu = love.graphics.getWidth()/2
    love.graphics.rectangle("fill", 0, 0, 100, 100)
end
